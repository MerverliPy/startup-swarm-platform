export default function Header() {
  return (
    <header style={{ paddingBottom: 24 }}>
      <h1>Startup Swarm Platform</h1>
      <p>
        GitHub-authenticated, multi-agent workflow platform with an optional Copilot-powered user workspace.
      </p>
    </header>
  );
}
